@tailwind base;
@tailwind components;
@tailwind utilities;

html, body {
  font-family: var(--wp--preset--font-family--roboto), Tahoma, sans-serif;
}

h1, h2, h3, h4, h5, h6 {
  font-family: var(--wp--preset--font-family--roboto-slab), Georgia, serif;
}
